import { StyleSheet } from 'react-native';

export const styledModal = StyleSheet.create({
  containerText: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    height: 100,
  },
  carouselContainer: {
    display: 'flex',
    flexDirection: 'row',
    gap: 30,
    margin: 20,
  },
});
