import { StyleSheet } from 'react-native';

export const styledAudio = StyleSheet.create({
  container: {
    display: 'flex',
    alignItems: 'center',
    flexDirection: 'row',
    paddingLeft: 20,
    paddingRight: 20,
    justifyContent: 'space-between',
    width: '100%',
  },
  containerRadius: {
    display: 'flex',
    alignItems: 'center',
    flexDirection: 'row',
  },
});
